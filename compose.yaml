version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - SECRET_KEY=dev-secret-key-change-in-production
    volumes:
      - .:/app
    command: python tissue_service/manage.py runserver 0.0.0.0:8000
